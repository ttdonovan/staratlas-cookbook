import React from 'react'
import ReactDOM from 'react-dom/client'
import WebWallet from './WebWallet.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WebWallet />
  </React.StrictMode>,
)
